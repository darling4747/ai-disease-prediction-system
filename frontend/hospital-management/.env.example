# Optional. Leave unset when the frontend and backend are served from the
# same public domain with the nginx /api proxy.
#
# Use this only when the backend is hosted on a separate public URL.
# Create React App reads this value at build time, so rebuild after changing it.
REACT_APP_API_URL=https://your-backend-domain.example.com
